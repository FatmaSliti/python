#Tuples are immutable (can't be changed or modified)
#used for data that never gonna change
coordinates = (4, 5)
# coordinates = [(4, 5), (6, 7), (8, 3)] #list of tuples

coordinates[1] = 10
print(coordinates[0])

